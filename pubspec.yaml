name: vn_studio
description: "VN Studio - 视觉小说创作工坊（Android 原生 APP）"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  path_provider: ^2.1.0
  share_plus: ^10.1.4
  file_picker: ^10.3.10
  archive: ^4.0.0
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
